# Normalize line endings to LF in the repository; check out native on Windows.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# Keep bun's lockfile out of language stats / diffs.
bun.lock linguist-generated=true
